#include <stdio.h>
#include <stdlib.h>

int k;

int main(){
    printf("inserire numero");
    scanf("%d", &k);
    }if(k > 0)(%k == 0) {
        printf("conferma");
    } else 

    return 0;   
#include<stdio.h>
#include<stdlib.h>

float P;
float L;

int main(){

    printf("inserisci primo numero");    
    scanf("%f &P);
    printf("inserisci secondo numero");
    scanf("%f &L);
    printf( "la somma dei due numeri è %f", L+P);
    printf( "la differenza dei due numeri è %f", L-P);
    printf( "il prodotto dei due numeri è %f", L*P);
    printf( "la divisione dei due numeri è %f", L/P);
    // in questo modo possono uscire numeri con la virgola
    
    return 0;


} 
